<% content_for(:title) { 'Who We Are' } %>
<h1>Who We Are</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= image_tag gravatar_helper(user.email) %>
      <%= link_to user.email, user_path(user) %>
    </li>
  <% end %>
</ul>